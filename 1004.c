#include <stdio.h>

int main() {
    int A, B, PROD;

    // Leitura dos valores inteiros
    scanf("%d %d", &A, &B);

    // Cálculo do produto
    PROD = A * B;

    // Impressão do resultado
    printf("PROD = %d\n", PROD);

    return 0;
}
